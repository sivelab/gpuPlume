#Specify root directory
ROOT = ../../../

#List all c++ files to compile
CFILES = Net_Controller.cpp Client_Controller.cpp

#List all header files to install
HDRS = Net_Controller.h Client_Controller.h

#List any extra libraries to link with
LOCAL_LIBS =

#Set Library name (if one)
LIBRARY = libClientController.a

#Set Program name (if one)
PROGRAM =

include $(ROOT)Make/Makefile.rules
