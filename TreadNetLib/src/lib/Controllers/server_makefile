#Specify root directory
ROOT = ../../../

#List all c++ files to compile
CFILES = Net_Controller.cpp Server_Controller.cpp

#List all header files to install
HDRS = Net_Controller.h Server_Controller.h

#List any extra libraries needed to link with
LOCAL_LIBS =

#Set Library name (if one)
LIBRARY = libServerController.a

#Set Program name (if one)
#PROGRAM =

include $(ROOT)/Make/Makefile.rules
